import { Stack } from "@mui/material";

const Topbar = () => {
  return <Stack direction={"row"} alignItems={"center"} justifyContent={"space-between"} spacing={3}></Stack>;
};

export default Topbar;
